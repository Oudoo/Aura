generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Suite {
  id       String    @id @default(uuid())
  suite    String
  suiteAr  String
  slug     String    @unique
  products Product[]
}

model Product {
  id          String @id @default(uuid())
  name        String
  nameAr      String
  slug        String @unique
  description String
  descAr      String
  status      String @default("PUBLISHED") // PUBLISHED, DRAFT
  order       Int    @default(0)
  features    String? @db.Text
  suiteId     String
  suite       Suite  @relation(fields: [suiteId], references: [id], onDelete: Cascade)
}

model Submission {
  id      String   @id @default(uuid())
  name    String
  email   String
  company String
  message String   @db.Text
  date    DateTime @default(now())
  status        String    @default("new")
  called        String    @default("no")
  notes         String?   @db.Text
  priority      String    @default("MEDIUM") // HOT, WARM, COLD, MEDIUM
  dealValue     Int       @default(0)
  source        String    @default("Website") // Website, Referral, LinkedIn, WhatsApp, Direct
  nextFollowUp  DateTime?
}

// Project Management Models
model Project {
  id          String   @id @default(uuid())
  title       String
  description String?  @db.Text
  clientName  String?  // links a project to a Client for the white-label portal
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  tasks       Task[]
}

model Task {
  id          String       @id @default(uuid())
  title       String
  description String?      @db.Text
  assignee    String       @default("Unassigned")
  status      String       @default("PENDING") // PENDING, IN_PROGRESS, DONE
  dueDate     DateTime?    // milestone deadline; powers "missed milestone" alerts
  projectId   String
  project     Project      @relation(fields: [projectId], references: [id], onDelete: Cascade)
  subTasks    SubTask[]
  comments    Comment[]
  attachments Attachment[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

model SubTask {
  id          String   @id @default(uuid())
  title       String
  isCompleted Boolean  @default(false)
  taskId      String
  task        Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Comment {
  id        String   @id @default(uuid())
  content   String   @db.Text
  author    String
  taskId    String
  task      Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
}

model Attachment {
  id        String   @id @default(uuid())
  url       String   @db.Text
  name      String
  taskId    String
  task      Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
}

// Finance & Invoicing
model Invoice {
  id          String   @id @default(uuid())
  invoiceNo   String   @unique
  clientName  String
  amount      Float
  status      String   @default("DRAFT") // DRAFT, SENT, PAID, OVERDUE
  issueDate   DateTime @default(now())
  dueDate     DateTime?
  notes       String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// Support & Ticketing
model Ticket {
  id          String   @id @default(uuid())
  title       String
  description String   @db.Text
  clientName  String
  status      String   @default("OPEN") // OPEN, IN_PROGRESS, RESOLVED, CLOSED
  priority    String   @default("MEDIUM") // LOW, MEDIUM, HIGH, URGENT
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// IAM – Admin Users
model AdminUser {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  name         String
  role         String   @default("VIEWER") // SUPER_ADMIN, ADMIN, VIEWER
  permissions  String?  @db.Text // JSON array: ["crm","finance","products","projects","support","analytics","iam"]
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

// Client Portal — white-label, read-only access for enterprise clients
model Client {
  id          String   @id @default(uuid())
  name        String   // must match Project.clientName / Invoice.clientName
  company     String?
  email       String?
  accessToken String   @unique // unguessable token used in the portal URL
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// Aura Health Score — daily operational health snapshots for trend analysis
model HealthSnapshot {
  id         String   @id @default(uuid())
  scope      String   @default("company") // "company" or a client name
  score      Int      // 0-100 overall
  breakdown  String   @db.Text // JSON: { delivery, finance, support, pipeline }
  capturedAt DateTime @default(now())

  @@index([scope, capturedAt])
}

// White-label / Tenant config
model TenantConfig {
  id             String   @id @default("default")
  companyName    String   @default("Aura")
  tagline        String?
  primaryColor   String   @default("#00E5FF")
  secondaryColor String   @default("#8B5CF6")
  logoUrl        String?
  fontHeading    String   @default("Plus Jakarta Sans")
  fontBody       String   @default("Inter")
  contactEmail   String?
  contactPhone   String?
  address        String?
  isDemo         Boolean  @default(false)
  updatedAt      DateTime @updatedAt
}
